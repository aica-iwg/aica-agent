# Ensure you have installed python3, python3-venv, python3-wheel, and libpq-dev
# via your OS package manager. You will also need to install docker and docker-compose

# build/test dependencies
bandit
flake8
safety
bashlint

# Not required for build/test but helpful for IDE completion/debugging
Django==3.2.12
django-extensions==3.1.3
celery==5.2.2
paramiko==2.10.1
pymongo==3.12.0
stix2==3.0.0
netifaces==0.11.0
yamllint==1.26.3